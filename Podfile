# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'petVideo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'
  pod 'SnapKit', '~> 4.0.0'

  # Pods for petVideo

  target 'petVideoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'petVideoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
